Marib R53 — إصلاح لوحة الصلاحيات: «المزامنة فشلت» اللي كانت كذبة
====================================================================================

الجولة 53 (16 سبتمبر 2026) — بلاغ المالك: «خانة الصلاحيات بيقولي
المزامنة فشلت». التشخيص: الشبكة سليمة (GET /api/perms بيرجع 200) —
الباج كان جوا الرندر نفسه وبيتخفى ورا رسالة فشل المزامنة.

  1. الجذر: renderPerms بينادي I18N.cur() — cur متغير خاص جوا
     i18n_core وعمرو ما اتصدّر → TypeError بيتلقط في catch الشبكة.
     الباج دخل مع R46 وعاش نقلات R50/R51/R52 (7 جولات).
  2. الإصلاح: I18N.is("ar") + فصل catch الرندر عن catch الشبكة
     (console.error + رسالة مستقلة pm_render_err عربي/إنجليزي/تركي).
  3. تنظيف: مفاتيح pm_empty_t/pm_empty_d/pm_add_user كانت متكررة
     مرتين من R51 — اتشال التكرار.
  4. cache-bust r52 → r53 (page.tsx + layout.tsx + skeleton.ts).

الاختبار: بناء نضيف + E2E متصفح بداتابيز 3 مستخدمين (مطوّر/أدمن/
يوزر): اللوحة بترندر كاملة (11 ميزة × أعمدة المستخدمين) · حفظ فعلي
(صف في marib_perm + أوديت perms:manpower.view) · توست «الصلاحية
اتغيرت» · صفر أخطاء كونسول · رسالة الفشل اختفت تمامًا.

الملفات المتغيرة (5 + توثيق):

  M  public/app/app_admin.js      — الإصلاح الرئيسي (I18N.is + فصل catch)
  M  public/app/i18n_dict.js      — pm_render_err + إزالة التكرار
  M  src/app/page.tsx             — cache-bust r53 (12 سكريبت)
  M  src/app/layout.tsx           — cache-bust r53 (CSS)
  M  src/app/skeleton.ts          — cache-bust r53 (أيقونة)
  M  docs/05-CURRENT-STATE.md     — حالة R53
  M  docs/06-ROUNDS.md            — سجل R53
  A  docs/سير-العمل-R53.md        — تقرير الجولة الكامل
