# سير العمل — الجولة 49
**تنظيف الـ dependencies (79→15) + مسح ملفات GitHub الميتة** — 16 سبتمبر 2026

> أول جولة تتسلم **مباشرة على GitHub** بتوكن مؤقت من المالك (بدون ZIP).
> التوكن اتطلب صلاحية Contents: Read+Write على الريبو ده بس — واتمسح بعد التسليم.

## السياق
R48 مسحت الكود الميت محليًا وسلمت ZIP + قائمة حذف. المالك رفع الـ ZIP (commit
d68c1c9) وطلب منا: (1) نحن اللي نمسح الـ 21 ملف من GitHub، (2) نعمل تنظيف
الـ dependencies اللي كنا قلنا عليها — وده كان ممنوع قبل كده (قاعدة
«package.json ممنوع يتلمس») فاتعمل **بإذن صريح** من المالك.

## R49-1 — التحقق من الرفع
- fetch بالتوكن → diff بين origin/main والحالة المحلية المختبرة (R48):
  **رفع المالك نزل كامل 100%** — الفرق الوحيد كان الـ 21 ملف الميتة + bun.lock قديم.

## R49-2 — مسح GitHub (commit ba4287a)
- الـ **21 ملف** (مش 20 — العد الفعلي طلع 21) في commit واحد:
  5 public/app + 1 html أوفلاين + 2 src/app + 7 routes + 3 lib + 3 seeds.
- التحقق بعد الدفع: الملفات بترجع 404 على GitHub API.
- DELETED-FILES-R48.txt اتحدّث بحالة «تمت».

## R49-3 — تنظيف الـ dependencies (بإذن المالك)
- **سكريبت تحليل آلي** (`scripts/r49-dep-analysis.py` خارج الريبو): يفحص كل حزمة
  ضد كل imports في src/ + الكونفيجات.
- النتيجة: **79 حزمة → 15 حزمة** (63 اتشالوا من node_modules).
- الفاضل:
  - dependencies (5): next · react · react-dom · pg · @electric-sql/pglite
  - devDependencies (10): typescript · @types/react · @types/react-dom ·
    @types/pg · bun-types · eslint · eslint-config-next · tailwindcss ·
    @tailwindcss/postcss · tw-animate-css
- **مصايد اتصادت قبل الحذف:**
  - `react-dom` طلعت «غير مستخدمة» (مفيش import صريح) — **أساسية**، فضلت.
  - `@dnd-kit/core` + `@reactuses/core` طلعوا «مستخدمين» بالغلط — كلمة `core`
    جوه `nextCoreWebVitals` في eslint.config — اتحذفوا.
  - `tailwindcss-animate` مستوردة بس في tailwind.config.ts — وTailwind v4 مش
    بيقرأ الملف ده أصلاً (مفيش `@config` في globals.css) → الملف + الحزمة اتشالوا مع بعض.
- حذف: `prisma/` (schema.prisma + schema.postgres.prisma) — كان فاضل عمدًا
  في R48 لقاعدة package.json؛ الإذن اتغير فاتشال.
- حذف سكريبتات `db:push/db:generate/db:migrate/db:reset` (Prisma CLI).
- إعادة تسمية الحزمة: `nextjs_tailwind_shadcn_ts` → `marib-performance`.

## R49-4 — الاختبار (قاعدة الـ Loop)
- `bun install`: 15 حزمة تتثبت في 230ms — «Removed: 63».
- `tsc --noEmit`: نضيف 100%.
- `eslint`: خطأ واحد **قديم وموروث** (no-sync-scripts في page.tsx — معمول
  بالتصميم ده عشان ترتيب إقلاع الـ SPA) — اتأكدنا إنه موجود قبل التغيير،
  وnext build مش بيـشغّل ESLint.
- `bun run build`: نجح — 15 routes زي ما هي.
- **E2E على سيرفر الإنتاج standalone** (في أمر واحد — شوف الملاحظات):
  - login Amin ✓ (200 + session + audit)
  - 10 routes حية كلها 200 — بما فيها `/api/manpower/export` (75KB Excel
    بمولّدنا الداخلي)
  - 9 مسارات/ملفات محذوفة كلها 404
  - `/api/health`: {ok:true, users:1, months:1, employees:810} — نفس رقم R48
  - صفر أخطاء سيرفر في اللوج

## ملاحظات تشغيل للجولات الجاية
- **السيرفر في بيئة الاختبار بيموت بين الأوامل حتى مع setsid** — الـ E2E
  لازم يشتغل في أمر واحد متكامل (start → sleep → curls → kill).
- الـ standalone محليًا: `rm -rf .next/standalone/db && cp -r db .next/standalone/db`
  (لو المجلد موجود قبل النسخة → نسخة متداخلة db/db والسيرفر يفشل).
- خطأ ESLent الموروث (no-sync-scripts + no-css-tags) — لو هيتصلح يومًا يبقى
  بـ strategy/script مع تحديث قواعد التجاهل، مش بإجبار async على السكريبتات
  (ترتيب الإقلاع مقدس).

## الملفات المتغيرة في R49
- `.gitignore` (جديد — اتضاف بعد ما `git add -A` كاد يرفع node_modules و .env
  محليًا؛ الريبو كان من غير gitignore خالص من أيام التيمبلت)
- `package.json` (اتنضف + rename) · `bun.lock` (اتولد جديد)
- محذوفات: `prisma/schema.prisma` · `prisma/schema.postgres.prisma` · `tailwind.config.ts`
- docs: 01/04/05/06 محدثين + `سير-العمل-R49.md` جديد + `سير-العمل.html`
  (بلوك R49 + الفوتر) + `CHANGED-FILES.txt` + `DELETED-FILES-R48.txt` (حالة تمت)

## التسليم
اتدفع مباشرة على main بـ commit منفصل بعد commit المسح — **مش محتاج ZIP**.
