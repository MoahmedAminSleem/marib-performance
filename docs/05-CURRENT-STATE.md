# 05 — CURRENT STATE

> **الحالة في 17 سبتمبر 2026 (R64).** ما تم، ما معلّق، المشاكل المعروفة.
> **R64:** قسم صلاحيات صفحة إدخال البيانات + الصفحة الرئيسية الجديدة
> (خلفية الدنيم + بارالاكس). آخر جولة هندسية قبلها: R63.

## ✅ مكتمل (R64 — قسم صلاحيات الإدخال + الصفحة الرئيسية)

| # | البند | الحالة |
|---|--------|--------|
| R64-1 | **قسم «صفحة إدخال البيانات»** في لوحة الصلاحيات: `entry.view` (فتح+قراءة) · `entry.edit` (حفظ/حذف/تيمبلتات) · `entry.po` (إدارة العقود) — 14 مفتاح إجمالًا، مستقلين تمامًا عن مفاتيح اللوحة | ✅ E2E |
| R64-2 | **الحرسان**: `requireEntryRead` = entry.view أو entry.edit أو entry.po · كتابة الإدخالات `entry.edit` · عقود /api/po `entry.po` (التسجيل التلقائي من نموذج الإنتاج جوه entry.edit) | ✅ |
| R64-3 | **المايجريشن (BOOT_VER 59)**: أي data.upload=edit قديم ← entry.view+edit+po — محدش خسر وصول (متحقق على قاعدة «قبل الترقية» حقيقية مزروعة بالـ PGlite) | ✅ |
| R64-4 | **عرض فقط**: entry.view بدون entry.edit = جداول بلا زراير كتابة + شارة «👁 عرض فقط» — والسيرفر بيصد برضه | ✅ UI+API |
| R64-5 | **الصفحة الرئيسية**: البوابة المرقاة — خلفية الدنيم المصورة (مرجع المالك، 93KB) + بارالاكس ماوس rAF-lerp + drift 64s + إضاءة بتتنفس + مسحة ضوء + حجاب مركزي + prefers-reduced-motion | ✅ |
| R64-6 | **الدخول الجديد → الرئيسية دايمًا**: `enterApp(fresh)` → البوابة بالترحيب باسم اليوزر · الريفرش بياخد آخر وضع زي ما هو | ✅ |
| R64-7 | **CODE-MAP قسم 7**: الأصول الثابتة (صور/css) بالأحجام والأدوار — خلفية الرئيسية بتتلاقى بالاسم | ✅ |

> **درس الجولة:** الصورة المولّدة اتفحصت بالفحص البصري (VLM) قبل الاعتماد
> — أول تدوير طلع فيه براندات زايدة (CARRERA JEANS) اتشالت بجولة
> تعديل مستهدفة. والمايجريشن اتفحص على قاعدة حقيقية قبل الترقية مش
> بـ "اتمنى إنه يشتغل" على قاعدة فاضية.

## ✅ مكتمل (R63 — الصلاحيات المتضاربة + rename + خريطة الكود)

| # | البند | الحالة |
|---|--------|--------|
| R63-1 | **حارس قراءة الإدخال** `requireEntryRead` (data.view **أو** data.upload): قراءة الإدخالات/الـ PO/التيمبلتات كانت محتاجة data.view — مسؤول إدخال مخبّية عنه اللوحة كان بيضرب 403 في كل حاجة | ✅ E2E 126/126 |
| R63-2 | **`/api/entries/employees`** (جديد): كومبوبوكس الإدخال بقى من مساره الخاص (كان /api/manpower المحتاج manpower.view) — الحد الأدنى بلا IDs | ✅ |
| R63-3 | **openUs بالصلاحية مش الدور**: يوزر مُنح users.manage كان شايف الزرار وممنوع من الفتح | ✅ |
| R63-4 | **savePerm صادق**: القيمة الأصلية قبل التحديث + رجوع حقيقي عند الفشل + رسالة 403 صح | ✅ |
| R63-5 | **صلاحيات حية**: watchPerms عند focus/visibility (reloadPerms كانت معمولة ومحدش بيناديها) + refreshChrome + مراجعة السطح (data-surface) | ✅ |
| R63-6 | **شارة «فعليًا:»** في لوحة الصلاحيات — inherit مش صندوق أسود | ✅ UI |
| R63-7 | **تعديل اسم المستخدم**: users PUT + تفرده 409 + الجلسة بتفضل (uid) + الكاش بيشيل الاسم (authcache RoleEntry) + زرار قلم في المودال + i18n ×3 | ✅ 8 فحوصات |
| R63-8 | **نظام ملاحة الكود**: `scripts/code_map.mjs` يولّد `docs/CODE-MAP.md` (6 أقسام + رادار التأثير) + بروتوكول `08-CODE-INTELLIGENCE.md` + فحص طزاجة في الـ E2E | ✅ node=bun |

> **درس الجولة الأهم:** الباجات كانت كلها في طبقة الواجهة/الدلالات
> مش في نظام الصلاحيات نفسه — السيرفر بيصد صح، بس الواجهة كانت
> بتكذب. والتوليد الآلي لازم يفرز بمقارن نصي صريح (readdirSync
> مختلف بين node وbun).

## ✅ مكتمل (R62 — Logging & Observability: قياس الاستعلامات + stats في /api/health)

| # | البند | الحالة |
|---|--------|--------|
| R62-1 | `stats.ts` (جديد): عدادات qCount/qMs/qSlow(250ms)/qErrors/srvErrors + lastError بسياقه — بدون passwords ولا قيم بارامترات | ✅ tsc 0 |
| R62-2 | `db.ts` timedQuery: قياس كل استعلام في مكان واحد (q + جمل المعاملة pg) — السجل البطيء: رأس SQL 80 حرف + عدد البارامترات | ✅ |
| R62-3 | `serverFail`: عداد 5xx + آخر خطأ `mod.method: message` — الرد بالبايت زي ما هو | ✅ |
| R62-4 | `/api/health stats`: uptime + q{count/slow/ms_total/errors} + srv_errors + last_error + mem.rss_mb + node — رابط واحد للتشغيل كله | ✅ عداد حي مثبت |
| R62-5 | الإثبات: E2E 50/50 — نفس 13 JSON + 5 XLSX بالبايت (التطبيع متجهز من R61) + فحوصات sinks (ملف + stdout) | ✅ صفر تغيير سلوك |

> توقيت الطلبات على مستوى المسار اتحط عن قصد بره النطاق — على
> Vercel بييجي من المنصة (Runtime Logs)، ومحليًا slow-query +
> ms_total بيغطوا الجزء البطيء فعلًا (القاعدة).

> درس الجولة: جهّز تطبيع الـ E2E قبل الميزة (stats كانت بتتشال من
> R61) — عشان كده إضافة الميزة عدّت من أول تشغيلة.

## ✅ مكتمل (R61 — توحيد الـ E2E: مكتبة مشتركة + baselines على git)

| # | البند | الحالة |
|---|--------|--------|
| R61-1 | `e2e_lib.sh`: كل الفحوصات الثابتة دوال (typecheck/seed/capture/compare/functional/UI) — سكريبت الجولة 66 سطر بدل 272 | ✅ E2E 80/80 |
| R61-2 | baselines (13 JSON + 5 XLSX) **مرفوعة على git** + bootstrap من الصفر (ensureBoot) — أي sandbox جديد شغال من غير تاريخ محلي | ✅ 16 مقارنة bootstrap خضرا |
| R61-3 | إثباتات جديدة: canary (المقارنة مش شكلية) + idempotence (نظافة + أوديت سجّل الأثر) + auth ضمن المقارنة (uid→<ID>) + تطبيع هويات manpower/users | ✅ |
| R61-4 | **🔧 باج حقيقي متصلح:** PGlite mkdir غير recursive — إقلاع self-hosted نضيف كان بيضرب ENOENT للأبد (اكتشفه فحص الـ bootstrap) | ✅ mkdirSync recursive |

> الأجندة الهندسية الدايمة: 1) Refactoring تمت (R52+R56+R59) ·
> 2) Optimize Performance تمت (R57) · 3) TS strict تمت (R60) ·
> 4) **E2E تمت (R61)** · 5) Logging تالية (الأساس جاهز من R25/R54
> — الجولة الجاية: عدادات استعلامات + slow-query + stats في
> /api/health).

> دروس الجولة: (1) اختبر أدوات الاختبار — الـ canary هو اللي بيثبت
> إن المقارنة مش فحص شكلي. (2) uuidات البذر والطوابع هويات مش
> بيانات — طبّعها بدل ما تستثنى الـ endpoint كله. (3) الفحص من
> الصفر بيكشف الفروض المخفية (باج mkdir كان مستخبي من 6 جولات).

## ✅ مكتمل (R60 — TypeScript strict: noUncheckedIndexedAccess)

| # | البند | الحالة |
|---|--------|--------|
| R60-1 | `noUncheckedIndexedAccess` مفعّل — 76 خطأ اتصلحوا بحراس حقيقية (`const old = cur[0]; if (!old) …` بدل فحص الطول) في 8 ملفات: manpower ×9 مواضع + po + entries + session + data + audit (flatMap) + manpower_io + xlsx-writer (for…of + `!` موثقة لفهرس CRC المقنّع) | ✅ tsc 0 أخطاء |
| R60-2 | علمان مجانيان: `noFallthroughCasesInSwitch` + `noImplicitOverride` (0 خطأ لكل منهما) + رفض موثق لـ `noPropertyAccessFromIndexSignature` (461 خطأ — بتحارب نمط body.field الديناميكي) | ✅ |
| R60-3 | الإثبات: E2E 42/42 — نفس 12 رد JSON + 5 ملفات XLSX متطابقة بالبايت مع baselines R59 + كل فحوصات R59 الوظيفية + UI smoke — الواجهة ما اتلمستش (v=r59 محفوظة) | ✅ صفر تغيير سلوك |

> الأجندة الهندسية الدايمة: 1) Refactoring تمت (R52+R56+R59) ·
> 2) Optimize Performance تمت (R57) · 3) **TS strict تمت (R60)** ·
> 4) E2E سكريبتات كل جولة + baseline/compare من R56 (تالية: توحيد
> الفحوصات المشتركة في ملف واحد بدل نسخ السكريبت) · 5) Logging
> الأساس جاهز من R25/R54.

> دروس الجولة: (1) فحص `.length` مش بينرّف `arr[0]` — الحارس على
> العنصر هو الـ idiom الصحيح. (2) اقيس قبل ما تخطط: التقدير القديم
> «~30+» طلع 76. (3) جرب أي علم جديد قبل الالتزام: المجاني خده
> والمكلف يوثق سبب رفضه.

## ✅ مكتمل (R59 — Refactoring: kit.js للواجهة + deleteEntry للـ API)

| # | البند | الحالة |
|---|--------|--------|
| R59-1 | `kit.js` (جديد): MaribKit — dstamp + ensureXLSX (بالوعد المخزن) + dlBlob + readGrid | ✅ E2E 38/38 |
| R59-2 | إبطال التكرار: 6 تنزيلات + 6 ختم تاريخ + 2 ensureXLSX + 2 قراءة شيت ← مصدر واحد | ✅ |
| R59-3 | **🔧 باج مكتشف ومصلح:** زرار رفع تيمبلت الغياب كان سايح دايمًا (MaribCloud.ensureXLSX عمرها ما اتصدّرت) — المسار بقى MaribKit.readGrid + الـ picker بقى مرفق بالـ DOM | ✅ رفع ملف فعلي من الواجهة |
| R59-4 | `deleteEntry` في entries.ts: الـ DELETE الثلاثة المتطابقة حرفيًا بقوا سطر واحد لكل مسار (نفس الجداول والأوديت والحارس) | ✅ 200/404/400/403 |
| R59-5 | الإثبات: 12 رد JSON + 5 ملفات XLSX متطابقة بالبايت مع لقطة الأساس (XLSX بكل أجزاء OOXML ما عدا طابع الإنشاء) | ✅ صفر تغيير سلوك |

> الأجندة الهندسية الدايمة: 1) Refactoring — ثلاث جولات تمت
> (R52 تقسيم app_main · R56 مكتبات API · R59 kit.js + deleteEntry) ·
> 2) Optimize Performance تمت (R57) · 3) TS strict تالية (البند المؤجل:
> `noUncheckedIndexedAccess`) · 4) E2E سكريبتات كل جولة +
> baseline/compare من R56 · 5) Logging الأساس جاهز من R25/R54.

> دروس الجولة: (1) المقارنة بالبايت لازم تعرف المتغير بالطبيعة
> (طوابع/uuid/خصائص المستند) وتطبّعه بدل الفشل الصاخب. (2) أي confirm()
> في مسار اختباري لازم يتعامل معاه فورًا — الديالوج المفتوح بيسكّر
> المتصفح كله بصمت للجولات الجاية. (3) المسح لـ refactoring بيكشف
> باجات منطقية قديمة (مسار ميت من يوم ما اتكتب) — مش مجرد تجميل.

## ✅ مكتمل (R58 — عقود الـ PO في الإدخال + أشرطة الخط/القسم + تاريخ امبارح)

| # | البند | الحالة |
|---|--------|--------|
| R58-1 | شريط للخط فوق + شريط للقسم تحت (نقر مباشر بدل الدروب ليست — نفس قيم R50) | ✅ E2E 36/36 |
| R58-2 | التاريخ الافتراضي في تسجيل الإنتاج = امبارح | ✅ |
| R58-3 | خانة PO حية: سطر العقد/المصنوع/المتبقي + النسبة وشريط تقدم + تولتيب تفصيلي (إنتاج القسم والخط المحددين بالتاريخ والإجمالي — بيتبع تغيير الشريطين) | ✅ |
| R58-4 | PO أول مرة → خانتا كمية العقد + المتبقي (تلقائي = العقد − المصنوع) — بتتسجل مع الحفظ | ✅ |
| R58-5 | `api/po` (جديد): قايمة/تفاصيل/تيمبلت إكسل/استيراد/upsert/حذف + جدول `marib_po` + فهرس تجميع (BOOT_VER=58) | ✅ |
| R58-6 | إدارة العقود من الموقع: عرض/تعديل الكمية في مكانها/إضافة/حذف (المرونة: تيمبلت أو كتابة مباشرة) | ✅ |
| R58-7 | الأذونات: قراءة data.view / كتابة data.upload edit + audit لكل عملية — الحارس متحقق (سحب الصلاحية = 403 فوري مع كاش R57) | ✅ |

> الأجندة الهندسية الدايمة: 1) Refactoring تمت (R52+R56) · 2)
> Optimize Performance تمت (R57) · 3) TS strict تالية (البند المؤجل:
> `noUncheckedIndexedAccess`) · 4) E2E سكريبتات كل جولة +
> baseline/compare من R56 · 5) Logging الأساس جاهز من R25/R54.

> درس الجولة: بناء SQL ديناميكي ببارامترات مرقّمة لازم يحسب الإزاحة
> (الـ PO بياخد $1 فالفلاتر تبدأ من $2) — والـ E2E المفصّل هو اللي كشف
> الـ bind mismatch ده قبل العميل. وتوقعات الفحص لازم تتبنى على
> السلوك الموثق (data.view افتراضي view لليوزر من R55) مش على الانطباع.

## ✅ مكتمل (R57 — Optimize Performance)

| # | البند | الحالة |
|---|--------|--------|
| R57-1 | بوابة الإقلاع السريع: استعلام واحد بدل ~44 DDL لكل نسخة باردة (`boot_ver` في marib_meta) + إثبات مرئي `boot:{path:fast\|full}` في /api/health | ✅ E2E 29/29 |
| R57-2 | كاش الدور/الصلاحيات 30 ثانية + إبطال فوري من users/perms PUT/DELETE (`lib/marib/authcache.ts`) — استعلامين أقل لكل نداء API (رحلتي شبكة على Neon) | ✅ إلزام فوري متحقق |
| R57-3 | `Cache-Control: immutable` سنة كاملة على `/app/*` (الـ 11 سكريبت + app.css + xlsx 932KB) — الزائر المتكرر = صفر تنزيل + v=r57 | ✅ |
| R57-4 | defer للـ 11 سكريبت (تنزيل متوازي + ترتيب التنفيذ محفوظ) + preload مكتبة الإكسل عند أول لمسة | ✅ UI E2E |
| R57-5 | استعلام lastSync محدود بشهور الموقع + فهرس `marib_audit(action, at)` بدل مسح الأوديت كله | ✅ متطابق بالبايت |
| R57-6 | **خلل مكتشف ومصلح:** POST /api/auth كان من غير ensureBoot — الدخول أول طلب على قاعدة فاضية كان بيفشل (relation does not exist) | ✅ فريش DB E2E |

> الأجندة الهندسية الدايمة: 1) Refactoring تمت (R52+R56) · **2)
> Optimize Performance تمت (R57)** · 3) TS strict تالية (البند
> المؤجل: `noUncheckedIndexedAccess` — ~30+ خطأ في مسارات API) ·
> 4) E2E سكريبتات كل جولة + baseline/compare من R56 · 5) Logging
> الأساس جاهز من R25/R54.

> درس الجولة (ساندبوكس): عملية السيرفر اسمها الفعلي `bun
> server.js` — `pkill -f "standalone/server.js"` عمره ما بيطابقها،
> فالسيرفرات الشردة كانت بتفضل ماسكة البورت وكل E2E بيختبر كود
> عتيق بصمت (EADDRINUSE بيموت في صمت). القاعدة: النمط الصح +
> انتظار فضي البورت فعليًا قبل البدء. ودرس القياس: رقم واحد في
> الساندبوكس مش دليل — الإحماء والوسيط هما الدليل (9.4ms «تراجع»
> طلع أثر إحماء؛ بعد إحماء كامل median=5.0ms ≈ baseline 4.7ms).

## ✅ مكتمل (R56 — Refactoring طبقة الـ API + إثبات Neon المرئي)

| # | البند | الحالة |
|---|--------|--------|
| R56-1 | سؤال المالك عن Neon: شرح مبسط + `/api/health` بيرجع `"db":"neon"` (إثبات مرئي إن الإنتاج على Neon) | ✅ E2E |
| R56-2 | `lib/marib/entries.ts` — إنهاء تكرار مسارات الإدخال (مطابقة الموظف/خرائط/تحقق الشهر والتاريخ) | ✅ متطابق بالبايت |
| R56-3 | `lib/marib/manpower_export.ts` — التصدير والتيمبلت اتنقلوا من الـ route (685→60 سطر) + حالة الموديول بقت سياق لكل طلب | ✅ XLSX متطابق بالبايت |
| R56-4 | استعلام ميت في deptRename اتمسح (COUNT بيتنفذ وبيترمي) | ✅ |
| R56-5 | توثيق 01/03/05/06 متحدث + `قاعدة-البيانات-شرح-بسيط.md` | ✅ |

> الأجندة الهندسية الدايمة: **1) Refactoring تمت (R52+R56)** ·
> 2) Optimize Performance تالية · 3) TS strict شغالة من R54
> (`noUncheckedIndexedAccess` مؤجل بوعي) · 4) E2E سكريبتات كل جولة
> + baseline/compare من R56 · 5) Logging الأساس جاهز من R25/R54.

> درس الجولة: الـ baseline قبل أي refactoring (لقطة XLSX بتتحمّل في
> دقيقة) + المقارنة بالبايت بتثبت «مش غيّر حاجة» أحسن من أي مراجعة
> بشرية. وفحص الواجهة لازم يعرف إزاي العنصر بيتخفي فعلًا (كلاس
> on/bye مش style.display).

## ✅ مكتمل (R55 — الصلاحيات بقت حقيقية على السيرفر)

| # | البند | الحالة |
|---|--------|--------|
| R55-1 | requirePerm/requirePermBody على كل مسارات البيانات (كانت data/manpower/settings/users/storage/perms بلا حارس صلاحيات) | ✅ E2E 22/22 |
| R55-2 | طبقة الواجهة: loadPerms/can — البوابة والأزرار بتسمع الصلاحية الفعلية | ✅ |

## ✅ مكتمل (R54 — لوجو مأرب + الوضع الفاتح + بذرة الهندسة)

| # | البند | الحالة |
|---|--------|--------|
| R54-1 | لوجو مأرب الأصلي رجع: التوب بار + شاشة الدخول + هيدر الاتزان (مكان الميزان) | ✅ E2E متصفح |
| R54-2 | رقعة الدخول رجعت عريضة 104×54 (كانت مربعة لأيقونة المصنع) | ✅ E2E |
| R54-3 | الوضع الفاتح: 8 فجوات نصوص على الأشرطة الغامقة اتسدت (brand/mp-tab/live/…) | ✅ computed+بكسل+VLM |
| R54-4 | TS strict أقوى: noImplicitAny + noUnusedLocals/Parameters (صفر أخطاء) | ✅ tsc نضيف |
| R54-5 | Logging: مسارا audit + health اتغطوا (زمن الفحص ms كمؤشر بطء) | ✅ 9/9 مسارات |
| R54-6 | cache-bust r53 → r54 (12 سكريبت + app.css) | ✅ 39 مرجع |

> **طلب دائم جديد من المالك (من R54):** كل جولة بعدها لازم تحمل
> Refactoring + Optimize Performance + TypeScript strict + E2E +
> Logging & Observability — كل واحدة بقدر ما تخص التغيير نفسه.
> مؤجل بوعي: `noUncheckedIndexedAccess` (~30+ خطأ في مسارات API —
> جولة refactoring لوحدها).

> درس الجولة: الثيم مش «خلفية» — كل عنصر بيرث متغيرات الألوان
> لازم يتقابل في الاتجاهين (نص غامق على فاتح / نص فاتح على الشريط
> الغامق المتعمد). وسيرفر يتيم ماسك بورت = عدو الاختبارات الصامتة
> (static جديد من الديسك + HTML قديم من الذاكرة) — تحقق من PID
> البورت بعد أي restart.

## ✅ مكتمل (R53 — إصلاح لوحة الصلاحيات)

| # | البند | الحالة |
|---|--------|--------|
| R53-1 | «المزامنة فشلت» في الصلاحيات: باج I18N.cur() من R46 — اتحل بـ I18N.is("ar") | ✅ E2E متصفح |
| R53-2 | فصل أخطاء الرندر عن أخطاء الشبكة في loadPerms + رسالة pm_render_err | ✅ درس مسجل |
| R53-3 | تنظيف مفاتيح pm_empty_* المكررة (R51) + cache-bust r53 | ✅ بناء+متصفح |

> درس الجولة: `.catch` واحد بيلقط fetch ورندر مع بعض = أخطاء الرندر
> بتتنكر كفشل شبكة. اللوحة كانت معطوبة من R46 والـ E2E ماشافش لأن
> الاختبارات كانت بتفحص الـ API مش الرندر.

## ✅ مكتمل (R52 — Refactoring هيكلي، صفر تغيير في السلوك)

| # | البند | الحالة |
|---|--------|--------|
| R52-1 | app_main.js اتقسم 4 وحدات (pages/entries/admin) عبر جسر __maribCtx | ✅ E2E متصفح |
| R52-2 | manpower/route.ts 876←476 + lib/marib/manpower_io.ts (استيراد حرفي) | ✅ round-trip+undo |
| R52-3 | مسح nodePath الميتة + 71 قاعدة CSS ميتة (grep-verified) | ✅ بناء+متصفح |
| R52-4 | cache-bust r52 على 12 سكريبت + layout + 18 أيقونة | ✅ 200s |
| — | باجي التحليل (TV/TS/TB + setAvPhoto/MaribMe) اتصطادوا بالـ E2E واتصلحوا | ✅ درس مسجل |

> درس الجولة: النداات جوه object-literal methods (`MaribMe = { set: fn }`)
> بتضلل تحليل «مين بينادي على مين» — وتعريفات multi-var في سطر واحد
> (`var T=…, TV=…`) بتضلل مسح الأسماء. الحذف «الميت» لازم يتدقق يدويًا
> + E2E إجباري قبل التسليم.

## ✅ مكتمل (R51 — صلاحيات + ثيمات مربعات + أيقونات 3D + تثبيت الأزرار)

| # | البند | الحالة |
|---|--------|--------|
| R51-1 | u2039 الحرفية اتصلحت + مسافة زرار الصلاحيات | ✅ متصفح |
| R51-2 | حالة الصلاحيات الفاضية: بطاقة + شرح أدوار + زرار إضافة مستخدم | ✅ متصفح |
| R51-3 | الثيمات مربعات ملونة 4 أرباع + ✓ للنشط (CSS المفقول اترجع) | ✅ داكن+فاتح |
| R51-4 | أيقونات Fluent 3D (MIT) في البوابة + الأزرار + اللوجو | ✅ 9/9 LOADED |
| R51-5 | رسومات الدخول المرسومة اتشالت (ماكينة/بكرة/خيوط) + لوجو مصنع 3D | ✅ متصفح |
| R51-6 | الأربعة مربعات (إعدادات/مستخدمين/بيانات/خروج) في كل الصفحات | ✅ بوابة+داشبورد+اتزان |
| — | TR/LTR + cache-bust r51 + صفر أخطاء كونسول | ✅ E2E |

> درس الجولة: `marib-app.css` اللي اتمسح في R48 كان فيه CSS حي (الثيمات)
> — أي مسح «كود ميت» لازم يتدقق إنه فعلًا ميت بالـ grep على الكلاسات.

## ✅ مكتمل (R50 — عربي/تركي + 8 طلبات المالك)

| # | البند | الحالة |
|---|--------|--------|
| R50-1 | إيموجي في كل الزراير (بدل SVG) | ✅ متصفح |
| R50-2 | عربي/تركي بس — عربي افتراضي RTL، الإنجليزي اتشال | ✅ E2E |
| R50-3 | شريطا التحديد بوب-أب (بالتحديد بس) | ✅ متصفح |
| R50-4 | كارت الزيادة = مجموع الأقسام الفعلية (3 مش 2) + تجميل الكروت | ✅ سيناريو حي |
| R50-5 | تجميل صفحات الزيادة والشواغر (.cc-row) | ✅ متصفح |
| R50-6 | تيمبلت 15 عمود بـ TR + name_tr/job_tr/label_tr | ✅ E2E |
| R50-7 | الشيت هو الحقيقة (removed + تحذير + undo) | ✅ E2E (810 → undo) |
| R50-8 | داتا إنتري: بطاقة بوابة + 5 أقسام × 5 خطوط + كومبوبوكس + إضافة كرقم | ✅ متصفح |
| — | الترجمة الفورية اتشالت بالكامل (طلب صريح) | ✅ 404 |

> ملاحظة تشغيل: أقسام الإنتاج الخمسة hardcoded في app_main.js
> (`ENT_SECTIONS`) · رفع تيمبلت قديم (13 عمود) لسه مدعوم.

## ✅ مكتمل (R49 — تنظيف dependencies + مسح GitHub)

| # | البند | الحالة |
|---|--------|--------|
| R49-1 | التحقق: رفع R48 على GitHub نزل كامل 100% | ✅ |
| R49-2 | مسح الـ 21 ملف الميتة من GitHub — commit واحد (ba4287a) | ✅ 404 متحقق |
| R49-3 | package.json: 79 → 15 حزمة (63 ميتة اتشالوا — بإذن المالك) | ✅ |
| R49-4 | حذف prisma/ + tailwind.config.ts + سكريبتات db:* | ✅ |
| R49-5 | tsc نضيف + build 15 routes + E2E standalone كامل | ✅ صفر أخطاء |

> **تسليم مباشر:** الجولة دي اتدفت على GitHub بالتوكن (commit منفصل بعد
> المسح) — **مفيش ZIP**. ملاحظة: خطأ ESLint الوحيد (no-sync-scripts في
> page.tsx) قديم وموروث ومصمم كده — مش من R49.

## ✅ مكتمل (R48 — Refactoring)

| # | البند | الحالة |
|---|--------|--------|
| R48-1 | مسح 8 ملفات ميتة من public/ و src/app (3.9MB) — صفر مراجع | ✅ متحقق 404 |
| R48-2 | مسح نظام Prisma القديم كامل (7 routes + 3 lib + 3 seeds) — نظام واحد | ✅ متحقق |
| R48-3 | /api/health على جداول marib الحية | ✅ E2E |
| R48-4 | إلغاء تكرار كود الترجمة + توحيد hasArabic | ✅ tsc + trSync E2E |
| R48-5 | إصلاح 11 خطأ أنواع موروثة — tsc --noEmit نضيف 100% | ✅ |
| R48-6 | cache-busting ?v=r48 | ✅ |
| R48-7 | بناء + curl E2E 35/35 على سيرفر الإنتاج standalone + متصفح E2E | ✅ |

> **تمت (R49):** الـ 21 ملف الميتة اتمسحوا من GitHub في commit واحد
> (ba4287a) — `DELETED-FILES-R48.txt` بقى سجل تاريخي بس.

## ✅ مكتمل (R47 — للمرجع)

| # | الميزة | الحالة |
|---|--------|--------|
| R47-1 | أيقونات التوب بار: لون صريح في SVG + ليبلات نصية + cache-busting `?v=r47` | ✅ متحقق بالبكسل |
| R47-2 | مودال الموظف: حقول name_ar/job_ar + حفظ تلقائي للعربي | ✅ E2E |
| R47-3 | التيمبلت + الاستيراد بأعمدة «بالعربي» + auto-preserve | ✅ E2E + Undo |
| R47-4 | كارت Extra Staff: العدد = مجموع excessNodes (توثيق بالدليل) | ✅ screenshots |
| R47-5 | استرجاع واجهة إدخال البيانات (كانت ضاعت من رفع v4) + إصلاح باج الحفظ + قسم/خط في فورم الإنتاج | ✅ E2E من المتصفح |
| R47-6 | الترجمة الفورية للأقسام (زرار AR live دايمًا ظاهر) | ✅ |
| R47-7 | bun run build ينجح بدون أخطاء (جاهز لـ Vercel) | ✅ |

## ✅ مكتمل (R46 — للمرجع)

نظام الصلاحيات الكامل (11 ميزة × matrix) · ترجمة فورية · بنية الأسماء ثنائية
اللغة · Import&Export موحد + undo 15 دقيقة · اختيار لغة Excel · صفحات إدخال
البيانات (backend) · تنظيف الإعدادات · الثيم الفاتح · كارت Extra Staff ·
ثبات الصفحة بعد الريفرش.

## ⚠️ معلّق / يحتاج متابعة

| الموضوع | التفاصيل |
|---------|----------|
| **بيانات الأسماء على الإنتاج** | الـ seed فيه أسماء عربي في عمود `name`. عشان الـ EN toggle يشتغل بالكامل: نزّل التيمبلت الجديد → اكتب الإنجليزي في «الأسم» والعربي في «الأسم بالعربي» → ارفع. أو عدّل يدوي من المودال (الحفظ التلقائي بيحمي العربي). |
| **حذف الملفات الميتة من GitHub** | الـ ZIP مش بيمسح — اتبع `DELETED-FILES-R48.txt` (كلهم في commit واحد أو مفيش). |
| **Vercel بعد R48** | البناء + الإنتاج المحلي standalone نجحوا. محتاج أول رفع فعلي للتأكيد. |
| **كاش الجولات الجاية** | في كل جولة: حدّث `?v=r48` → `?v=rNN` في `layout.tsx` + `page.tsx`. |

## 🔴 المشاكل المعروفة

1. **dev server instability:** الحل: `pkill -9 -f "next"` ثم إعادة التشغيل.
2. **PGlite init:** أحياناً بتفشل. الحل: `rm -rf db/pglite` (الزرع بيرجع تلقائيًا).
3. **⚠️ PGlite multi-process:** ممنوع فتح PGlite instance تاني على نفس `db/pglite`
   والسيرفر شغال (سكريبتات الزرع المباشرة كده بتخرب الـ visibility). الاسترجاع
   من الـ API دايمًا أو وقّف السيرفر الأول. (الإنتاج Postgres/Neon — مش متأثر.)
4. **الترجمة في بيئة محلية معزولة:** Google gtx / MyMemory ممكن يردوا 429 من
   IP مشترك — على Vercel شغالين عادي.

## 📦 الإصدارات

| Version | Date | Files | Notes |
|---------|------|-------|-------|
| R53 | 16 Sep 2026 | 5 + docs | إصلاح «المزامنة فشلت» — باج I18N.cur كامن من R46 |
| R47 | 16 Sep 2026 | 9 + docs | كل المعلّق + استرجاع كود ضاع + cache-busting |
| v4 | 16 Sep 2026 | 20 | excess card fix + defensive CSS + page persistence |
| v1-v3 | 15-16 Sep 2026 | 16-18 | R46 تدريجي (v1 كان فيه واجهة الإدخال اللي رجعناها في R47) |

## الخطوة التالية

كل معلّقات R46 اتنفذت في R47. المقترح:
1. ارفع R47 على GitHub وجرّب على الموقع الحقيقي (الأيقونات هتظهر حتى من غير
   hard refresh بفضل الـ cache-busting).
2. لو عايز تنقل الأسماء العربية القديمة جملة واحدة → استخدم التيمبلت الجديد.
3. تحسينات محتملة: ربط صفحات الإدخال بالرسومات، bulk نقل أسماء.
